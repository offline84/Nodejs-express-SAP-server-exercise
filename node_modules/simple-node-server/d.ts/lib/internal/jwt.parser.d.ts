import * as jwtUtil from 'jsonwebtoken';
import { NodeletRequest } from '../classes/nodelet.request';
import { UserPrincipal } from '../classes/user.principal';
export declare class JwtParser {
    jwtError: jwtUtil.JsonWebTokenError;
    private cookieParser;
    extractUserPrincipalFromToken(nodeletRequest: NodeletRequest): UserPrincipal;
    /**
     * extract json web token from header or cookie
     * @param headers
     */
    private extractJsonWebToken(headers);
    /**
     * try extract token from header
     * @param headers
     */
    private extractFromHeader(headers);
    private extractFromCookie(headers);
    /**
     * handle json web token
     */
    private handleJsonWebToken(jsonWebToken);
    /**
     * create userPrincipal
     * @param jsonWebTokenDecoded
     */
    private createUserPrincipal(jsonWebTokenDecoded);
}

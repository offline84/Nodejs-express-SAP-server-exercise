import { HttpHeaderCookie } from '../interfaces/http.header.cookie';
export declare class CookieParser {
    private cookieString;
    private parsedCookie;
    constructor(cookieString: string);
    private parse();
    private extract(data);
    getParsedCookie(): HttpHeaderCookie;
}

import { NodeletRequest } from '../classes/nodelet.request';
import { NodeletResponse } from '../classes/nodelet.response';
/**
 * handle file requests
 * @class FilePipe
 */
export declare class FilePipe {
    private resourcePath;
    private viewPath;
    constructor(resourcePath: string, viewPath: string);
    name(): string;
    /**
     * create readstream from file
     * @param nodeletRequest
     * @return Promise<ReadStream> file
     */
    createReadStreamFromRequest(nodeletRequest: NodeletRequest): Promise<NodeletResponse>;
    private determinePath(requestPath);
    /**
     * @Getter
     * @return resourcePath string
     */
    getResourcePath(): string;
}

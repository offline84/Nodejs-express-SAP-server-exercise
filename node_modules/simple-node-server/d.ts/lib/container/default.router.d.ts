import { MappingOption } from '../classes/mapping.option';
import { NodeletRequest } from '../classes/nodelet.request';
export default class Router {
    static context: any[];
    static mappings: MappingOption[];
    private static colorMapping;
    static register(instance: any): void;
    static fetch(NAME: string): any;
    static addMapping(mapping: MappingOption): void;
    static getMapping(nodeletRequest: NodeletRequest): MappingOption;
    private static reduceMappings(actual, current, nodeletRequest);
    private static rankMatch(match, url, paths);
    private static validateMethod(mapping, method);
}

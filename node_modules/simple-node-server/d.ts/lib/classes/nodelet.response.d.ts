/// <reference types="node" />
import { ReadStream } from 'fs';
import { HttpStatus } from '../enums/http.status';
import { MimeType } from '../enums/mime.type';
import { HttpAuthorization } from '../interfaces/http.authorization';
export declare class NodeletResponse {
    body: string | {} | ReadStream;
    produces: MimeType;
    headers: Map<string, string>;
    authorization: HttpAuthorization;
    httpStatus: HttpStatus;
    constructor(nodeletResponse?: {
        body: string | {};
        produces: MimeType;
        headers: Map<string, string>;
        authorization: HttpAuthorization;
        httpStatus: number;
    });
    isRedirect(): boolean;
    isFile(): boolean;
    getProduces(): MimeType;
    createSimple(body: any): NodeletResponse;
    createError(err: Error | string): NodeletResponse;
    createRedirect(): NodeletResponse;
    toString(): string;
}

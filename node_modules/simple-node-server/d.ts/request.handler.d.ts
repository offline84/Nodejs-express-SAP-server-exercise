/// <reference types="node" />
import * as http from 'http';
import { NodeletRequest } from './lib/classes/nodelet.request';
export declare class RequestHandler {
    getNodeletRequest(request: http.IncomingMessage): Promise<NodeletRequest>;
    private parseBody(body, contenttype);
    private parseFormUrlEncoded(body);
    private parseJson(body);
    private parsePath(path);
}

import { MappingOption } from './lib/classes/mapping.option';
import { NodeletRequest } from './lib/classes/nodelet.request';
import { UserPrincipal } from './lib/classes/user.principal';
import { ValidMap } from './lib/interfaces/valid.map';
export declare class RequestAuthorization {
    private jwtParser;
    private renewTime;
    key: string;
    /**
     * @Getter authorize
     */
    authorize(nodeletResquest: NodeletRequest, mapping: MappingOption): ValidMap;
    /**
     * verify json web token
     * @param jsonWebToken
     * @param role
     */
    private verifyRequest(mAuth, userPrincipal, token);
    private createRedirectNodeletResponse(mapping);
    /**
     * renew token timestamp
     * @param jsonWebToken
     */
    renewToken(userPrincipal: UserPrincipal): string;
}
